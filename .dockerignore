# Ignore docker files
Dockerfile
.dockerignore

# Ignore node_modules and logs (handle it in the Dockerfile)
node_modules
npm-debug.log

# Ignore aws-cdk (not part of this task) & dist folder (handle it in the Dockerfile)
aws-cdk
dist